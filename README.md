# Ads Reporting Dashboard

A Python-based reporting tool that connects to Google Ads and Facebook Ads to extract key performance metrics and present them in an interactive Streamlit dashboard.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Run the dashboard:
   ```bash
   streamlit run main.py
   ```
